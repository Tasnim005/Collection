**To run your C code every time you write it** in VS Code:

---

## Manual Compile + Run


```c
gcc filename.c -o filename.exe
.\filename.exe
```

**Example:**

```c
gcc main.c -o main.exe
.\main.exe
```

> You must be in the correct folder where your `.c` file is saved.

---

